<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>首页</title>
	<link rel="stylesheet" href="/stylesheets/index.css" />
</head>
<body>
	<%- include('header'); %>
	<ul class="comment">
		<% for( var i = 0; i < result.length; i++) { %>
			<li class="comment-list"><%= result[i].comment %></li>
		<% } %>
	</ul>
		
	<ul class="page">
		<% for( var i = 1; i <= pageNum; i++) { %>
			<li class="page-num"><a href="/?page=<%= i %>"><%= i %></a></li>
		<% } %>
	</ul>
</body>
</html>